package templates

import "strconv"

templ Loop(place int) {
	<div
		id="newnote"
		hx-swap="outerHTML"
		hx-trigger="revealed delay:500ms"
		hx-target="this"
		hx-get={ "/GETLOOP/" +
	strconv.Itoa(place) }
	>Loading next one...</div>
}
