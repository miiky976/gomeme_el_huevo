package templates

import (
	"miiky976/Godis/utils"
)

templ Image(key, head string, image []byte) {
	<div class="item image">
		<h6>{ key }</h6>
		<img src={ utils.Source(head, image) }/>
		<button class="copy">Copy</button>
	</div>
}
